<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.asdf1234">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Asdf1234"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity android:name=".ui.InformationActivity"></activity>
        <activity android:name=".ui.tasks.CreateTask" />
        <activity android:name=".ui.profile.ChangePassActivity" />
        <activity android:name=".ui.profile.AccountActivity" />
        <activity android:name=".ui.tasks.ShowTask" />
        <activity android:name=".ui.events.AddTaskToEvent" />
        <activity android:name=".ui.events.UpdateEvent" />
        <activity android:name=".ui.events.ShowEvent" />
        <activity android:name=".ui.events.CreateEvent" />
        <activity android:name=".ui.auth.VerifyActivity" />
        <activity android:name=".ui.auth.RegisterActivity" />
        <activity android:name=".ui.auth.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
    </application>

</manifest>